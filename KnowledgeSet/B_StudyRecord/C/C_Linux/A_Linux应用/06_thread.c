// C std lib
// 使用时去查即可
// #include <***.h>

/* 线程的部分操作
    pthread_t pid;
    if (pthread_create(&pid, NULL, thread_body, NULL) == -1)
        perror("thread create fail\n");
    pthread_join(pid, NULL);
    pthread_cancel(pid);
 */



